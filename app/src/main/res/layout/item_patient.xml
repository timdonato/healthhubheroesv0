<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Circle Avatar Icon -->
    <TextView
        android:id="@+id/avatarIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:backgroundTint="#E1D6F9"
        android:background="@drawable/circle_background"
        android:gravity="center"
        android:text="A"
        android:textColor="#6B4DA5"
        android:textSize="20sp" />

    <!-- Patient Name -->
    <TextView
        android:id="@+id/textPatientName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/avatarIcon"
        android:layout_marginStart="16dp"
        android:text="Ashish Mehra"
        android:textColor="#000000"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Patient Age -->
    <TextView
        android:id="@+id/textPatientAge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textPatientName"
        android:layout_alignStart="@id/textPatientName"
        android:text="Age: 32"
        android:textColor="#888888"
        android:textSize="14sp" />

    <!-- Edit Icon -->
    <ImageView
        android:id="@+id/iconEdit"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:src="@drawable/baseline_mode_edit_24"
        android:contentDescription="@string/tvEditPatient" />

</RelativeLayout>
